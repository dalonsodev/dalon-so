---
import Container from './Container.astro'

interface Props {
  id: string
}

const { id } = Astro.props
---

<section id={id} class="py-16" aria-labelledby={id + '-heading'}>
  <Container>
    <slot />
  </Container>
</section>
